"use client";

// Back-compat shim. The real implementation moved to
// `@/components/ClientProviders`; new code should import that directly.
export { ClientProviders as Providers } from "@/components/ClientProviders";
